# Paper package — extracted 2026-06-22

The self-contained paper has been extracted to **`gad-paper/`** — copy/move it out
and `git init` it as the new writing-only repo. It compiles standalone
(`cd gad-paper && pdflatex paper.tex`, 21 pp) with no dependency on this repo.

See `gad-paper/README.md` for the manifest, headline, and gaps to fill.

Provenance lives here in `GAD_plus`:
- Report source of truth: `BENCHMARK_REPORT_2026-05-16.tex` + `figures_2026_05_16/` + `scripts/build_pdf_2026_05_16.py`
- Coordinate-step finding: `FINDING_CART_VS_MW_STEP_2026_06_10.md`
- Raw runs: `/lustre07/scratch/memoozd/gadplus/runs/`

Settled: SCINE dropped; Cartesian step + MW projection canonical (≤0.35 pp effect, reported numbers stand); hybrid numbers valid.
